# 项目实训08｜会话记忆与天气服务

> **训练目标**：实现"多轮对话记忆"——Redis 持久化 + 内存降级的会话管理；接入 OpenWeather 天气接口。
> **预计时长**：6 学时 ｜ **前置条件**：实训 07 完成
> **本次交付物**：`core/memory_manager.py`、主控的记忆接入、`services/weather_service.py`、`routes/weather_router.py`

---

## 一、知识点

### 1. 为什么需要会话记忆

"它叶子发黄怎么办？"——"它"指什么？没有记忆，多轮对话无法成立。方案对比：

| 方案 | 优点 | 缺点 |
|------|------|------|
| 进程内字典 | 零依赖 | 多 worker/重启即丢失 |
| **Redis** | 跨进程共享、重启不丢、天然 TTL | 需要部署一个服务 |

本项目：**Redis 优先，自动降级内存**——开发与生产用同一套代码。

### 2. 记忆的"成本控制"

历史消息会全部进提示词，必须裁剪：

- 存储上限 10 条（`LTRIM`）；
- 入提示词前再裁剪到 **≤5 条 / ≤1500 字符**；
- TTL 7 天自动过期。

### 3. session_id 的流转

前端生成（`crypto.randomUUID()`）→ 每次请求随 FormData 带上 → 后端以 `agrigpt:session:{sid}` 为键读写。**后端无状态，状态全在 Redis**——这是可水平扩展的关键。

---

## 二、操作步骤

### 步骤 1：准备 Redis（任选其一）

```powershell
# 方式 A：Docker（推荐）
docker run -d --name agrigpt-redis -p 6379:6379 redis:7-alpine

# 方式 B：Windows 本机安装（Memurai 或 WSL 内 apt install redis）
```

验证：`docker exec agrigpt-redis redis-cli ping` → `PONG`

确认 `.env`：

```dotenv
REDIS_URL=redis://localhost:6379/0
```

### 步骤 2：实现记忆管理器 `backend/core/memory_manager.py`

```python
"""
Chat memory manager with Redis (persistent) or in-memory fallback.
Redis enables: multi-worker scaling, persistence across restarts.
"""
from __future__ import annotations

import json
import collections
from typing import List, Dict, Any, Optional

from backend.core.config import settings

MAX_HISTORY_LENGTH = 10
MAX_HISTORY_MESSAGES = 5
MAX_HISTORY_CHARS = 1500

_KEY_PREFIX = "agrigpt:session:"
_KEY_TTL_SECONDS = 86400 * 7  # 7 days

# In-memory fallback when Redis unavailable
_CHAT_MEMORY: Dict[str, collections.deque] = {}

_redis_client: Optional[Any] = None


def _get_redis():
    """Lazy Redis connection; returns None if not configured or connection fails."""
    global _redis_client
    if _redis_client is not None:
        return _redis_client
    url = str(settings.REDIS_URL or "").strip()
    if not url or url.lower() in ("", "none", "false"):
        return None
    try:
        import redis
        client = redis.from_url(
            url,
            decode_responses=True,
            socket_connect_timeout=2,
        )
        client.ping()
        _redis_client = client
        return _redis_client
    except Exception:
        _redis_client = None
        return None


def _trim_history(history: List[Dict[str, str]]) -> List[Dict[str, str]]:
    """Trim to last N messages and ~N chars for token efficiency."""
    if not history:
        return []
    trimmed = history[-MAX_HISTORY_MESSAGES:]
    total = 0
    out = []
    for msg in reversed(trimmed):
        content = str(msg.get("content", "") or "")
        total += len(content) + 20
        if total > MAX_HISTORY_CHARS and out:
            break
        out.insert(0, msg)
    return out


def get_chat_history(session_id: str) -> List[Dict[str, str]]:
    """
    Retrieve chat history for a session, trimmed for prompt efficiency.
    Uses Redis if configured, else in-memory.
    """
    if not session_id:
        return []

    r = _get_redis()
    if r:
        try:
            key = f"{_KEY_PREFIX}{session_id}"
            raw = r.lrange(key, -MAX_HISTORY_LENGTH, -1)
            if not raw:
                return []
            history = []
            for m in raw:
                if not m:
                    continue
                try:
                    history.append(json.loads(m))
                except (json.JSONDecodeError, TypeError):
                    continue
            return _trim_history(history)
        except Exception:
            global _redis_client
            _redis_client = None

    # In-memory fallback
    if session_id not in _CHAT_MEMORY:
        return []
    raw = list(_CHAT_MEMORY[session_id])
    return _trim_history(raw)


def add_message_to_history(session_id: str, role: str, content: str) -> None:
    """
    Add a message to the session's history.
    Uses Redis if configured, else in-memory.
    """
    if not session_id:
        return

    msg = {"role": role, "content": str(content or "")}

    r = _get_redis()
    if r:
        try:
            key = f"{_KEY_PREFIX}{session_id}"
            r.rpush(key, json.dumps(msg))
            r.ltrim(key, -MAX_HISTORY_LENGTH, -1)
            r.expire(key, _KEY_TTL_SECONDS)
            return
        except Exception:
            global _redis_client
            _redis_client = None

    # In-memory fallback
    if session_id not in _CHAT_MEMORY:
        _CHAT_MEMORY[session_id] = collections.deque(maxlen=MAX_HISTORY_LENGTH)
    _CHAT_MEMORY[session_id].append(msg)


def format_history_for_prompt(history: List[Dict[str, str]]) -> str:
    """Convert list of messages into a string for LLM context."""
    if not history:
        return "No previous conversation."
    formatted = []
    for msg in history:
        role = str(msg.get("role", "user")).upper()
        content = str(msg.get("content", "") or "")
        formatted.append(f"{role}: {content}")
    return "\n".join(formatted)


def redis_available() -> bool:
    """True if Redis is configured and reachable."""
    return _get_redis() is not None
```

**设计要点**：

1. **懒连接 + 失败置空**：Redis 挂了不会拖垮主流程，下一个请求自动重试连接；
2. `decode_responses=True`：直接拿字符串，省去到处 `bytes.decode`；
3. **写路径三步**：`RPUSH` 追加 → `LTRIM` 截尾 → `EXPIRE` 续期。

### 步骤 3：主控接入记忆

修改 `master_agent.py`：

```python
from backend.core.memory_manager import (
    get_chat_history,
    add_message_to_history,
    format_history_for_prompt,
)
```

`route_query` 中，把实训 05 的占位行替换为：

```python
    chat_history_list = get_chat_history(session_id)
    chat_history_str = format_history_for_prompt(chat_history_list)
```

纯图片分支末尾补记忆写入：

```python
        if session_id:
            add_message_to_history(session_id, "user", "Uploaded an image")
        response = registry["FormatterAgent"].handle_query(payload, **agent_kw)
        if session_id:
            add_message_to_history(session_id, "assistant", response)
        return response
```

正常文本分支在拿到 `formatted_response` 之后：

```python
    if session_id:
        add_message_to_history(session_id, "user", clean_query)
        add_message_to_history(session_id, "assistant", formatted_response)
```

### 步骤 4：实现天气服务与路由

`backend/services/weather_service.py`：

```python
import requests
from backend.core.config import settings

OPENWEATHER_URL = "https://api.openweathermap.org/data/2.5/weather"

def get_current_weather(lat: float, lon: float) -> dict:
    if not settings.OPENWEATHER_API_KEY:
        return {"error": "Weather service not configured"}

    params = {
        "lat": lat,
        "lon": lon,
        "appid": settings.OPENWEATHER_API_KEY,
        "units": "metric",
    }

    try:
        res = requests.get(OPENWEATHER_URL, params=params, timeout=5)
        data = res.json()

        if res.status_code != 200:
            return {"error": "Unable to fetch weather"}

        weather_list = data.get("weather") or []
        if not weather_list or not isinstance(weather_list[0], dict):
            return {"error": "Invalid weather response"}

        condition_raw = str(weather_list[0].get("main", "clear")).lower()

        condition = (
            "rainy" if "rain" in condition_raw else
            "cloudy" if "cloud" in condition_raw else
            "sunny"
        )

        main = data.get("main") or {}
        wind = data.get("wind") or {}
        return {
            "location": data.get("name", "Your Location"),
            "temp": round(float(main.get("temp", 0))),
            "humidity": int(main.get("humidity", 0)),
            "wind": round(float(wind.get("speed", 0)) * 3.6),
            "condition": condition,
        }

    except Exception:
        return {"error": "Weather API unavailable"}
```

`backend/routes/weather_router.py`：

```python
from fastapi import APIRouter, Query
from backend.services.weather_service import get_current_weather

router = APIRouter(prefix="/weather", tags=["Weather"])

@router.get("/current")
def current_weather(
    lat: float = Query(...),
    lon: float = Query(...)
):
    return get_current_weather(lat, lon)
```

在 `main.py` 挂载，并完善启动日志（报告 RAG 与 Redis 的实际工作模式）：

```python
from backend.routes.weather_router import router as weather_router
app.include_router(weather_router)
```

```python
    # Log Pinecone vs FAISS for RAG
    if settings.PINECONE_API_KEY and settings.PINECONE_INDEX_NAME:
        print("[Pinecone] Configured for RAG (SubsidyAgent).")
    else:
        print("[RAG] Using FAISS (local). Set PINECONE_API_KEY + PINECONE_INDEX_NAME for Pinecone.")

    # Log Redis vs in-memory for chat memory
    from backend.core.memory_manager import redis_available
    if redis_available():
        print("[Redis] Chat memory: persistent (redis)")
    else:
        print("[Redis] Chat memory: in-memory (set REDIS_URL for persistent storage)")
```

> 没有 OpenWeather Key 也不影响其他功能——接口会返回 `{"error": "Weather service not configured"}`，这就是优雅降级。

### 步骤 5：验证多轮记忆

```powershell
# 第一轮
curl.exe -X POST http://localhost:8000/ask/text -F "query=I grow tomatoes." -F "session_id=mem-test"

# 第二轮（代词指代）
curl.exe -X POST http://localhost:8000/ask/text -F "query=How often should I water it?" -F "session_id=mem-test"
```

预期：第二轮回答明确针对**番茄**给出灌溉建议（路由日志中上下文已注入）。

验证 Redis 数据：

```powershell
docker exec agrigpt-redis redis-cli LRANGE agrigpt:session:mem-test 0 -1
```

降级验证：停掉 Redis（`docker stop agrigpt-redis`）再提问 → 服务正常，启动日志变为 `in-memory`。

天气验证（有 Key 时）：

```
http://localhost:8000/weather/current?lat=13.08&lon=80.27
```

---

## 三、验收标准

| # | 检查项 | 通过标准 |
|---|--------|----------|
| 1 | 同 session 两轮对话 | 第二轮能解析代词指代 |
| 2 | 不同 session | 上下文互相隔离 |
| 3 | 重启后端后再提问（同 session） | Redis 模式下历史仍在 |
| 4 | 停止 Redis | 服务不崩，自动降级内存 |
| 5 | `/weather/current` | 返回温度/湿度/风/天气状况（或明确 error 文案） |

---

## 四、常见问题排查

| 现象 | 原因与解决 |
|------|-----------|
| 记忆没生效 | 两次请求的 `session_id` 不一致；或走了内存模式且重启过 |
| `redis.exceptions.ConnectionError` | `REDIS_URL` 端口错误；Docker 未映射 6379 |
| 提示词里历史太长 | 检查 `_trim_history` 是否被绕过——所有读取必须经 `get_chat_history` |
| 天气 401 | Key 未激活（新注册需等几分钟）或未勾选 "Current Weather Data" |

---

## 五、拓展任务（选做）

1. 给会话增加"摘要压缩"：当历史超过 10 条时，用 LLM 生成一句摘要替代旧消息；
2. 把 `MAX_HISTORY_MESSAGES`、`TTL` 等常量迁入 `core/config.py` 做成环境变量；
3. 写 pytest：mock `_get_redis` 返回 None，验证内存回退路径读写一致。

> **下一节预告**：实训 09 构建指标与反馈体系，让系统"可运营、可度量"。
