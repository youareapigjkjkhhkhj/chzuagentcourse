# 项目实训09｜指标反馈与运维端点

> **训练目标**：构建运营指标体系——用户反馈收集、用量/质量指标聚合、依赖全景健康检查；（可选）接入 LangSmith 追踪。
> **预计时长**：6 学时 ｜ **前置条件**：实训 08 完成
> **本次交付物**：`services/feedback_service.py`、`routes/metrics_router.py`、升级版 `health_router.py`

---

## 一、知识点

### 1. AI 应用的"双指标"体系

| 维度 | 指标 | 数据来源 |
|------|------|----------|
| **用量**（Usage） | 总请求、按 Agent 分布、按类型（文本/图片/多模态）、按天趋势 | `query_log.json`（实训 04 已埋点） |
| **质量**（Quality） | 点赞/点踩数、满意度 = positive / total | `feedback_log.json`（本次新增） |

> 回顾：实训 04 让 Agent 基类**强制落日志**、实训 05 在 `meta` 记录参与 Agent——都是在为本次的指标聚合铺路。企业里这叫"埋点先行"。

### 2. 反馈如何关联到具体回答

前端每次请求拿到 `request_id` → 用户点赞/踩时把 `request_id + feedback` 回传 → 后端追加到 `feedback_log.json`。`request_id` 是贯穿全链路的"关联键"。

---

## 二、操作步骤

### 步骤 1：实现反馈服务 `backend/services/feedback_service.py`

```python
"""Feedback service - stores user quality ratings for metrics."""
from __future__ import annotations

import json
import threading
from datetime import datetime
from pathlib import Path
from typing import List

BASE_DIR = Path(__file__).resolve().parent.parent
DATA_DIR = BASE_DIR / "data"
FEEDBACK_PATH = DATA_DIR / "feedback_log.json"
FEEDBACK_TMP = DATA_DIR / "feedback_log_tmp.json"

DATA_DIR.mkdir(parents=True, exist_ok=True)
_lock = threading.Lock()


def record_feedback(request_id: str, feedback: str, source: str = "chat") -> None:
    """
    Record user feedback (positive/negative) for a request.
    source: "chat" | "image"
    """
    entry = {
        "request_id": request_id,
        "feedback": feedback,  # "positive" | "negative"
        "source": source,
        "timestamp": datetime.utcnow().isoformat(),
    }
    with _lock:
        try:
            data = []
            if FEEDBACK_PATH.exists():
                with open(FEEDBACK_PATH, "r", encoding="utf-8") as f:
                    data = json.load(f)
                    if not isinstance(data, list):
                        data = []
            data.append(entry)
            with open(FEEDBACK_TMP, "w", encoding="utf-8") as f:
                json.dump(data, f, indent=2)
            FEEDBACK_TMP.replace(FEEDBACK_PATH)
        except Exception as e:
            print(f"[FEEDBACK] Failed to write: {e}")


def get_feedback_log() -> List[dict]:
    """Read all feedback entries."""
    with _lock:
        try:
            if not FEEDBACK_PATH.exists():
                return []
            with open(FEEDBACK_PATH, "r", encoding="utf-8") as f:
                data = json.load(f)
                return data if isinstance(data, list) else []
        except Exception:
            return []
```

> 与 `history_service` 相同的原子写模式——同一套写法在团队内复用，降低心智负担。

### 步骤 2：实现指标路由 `backend/routes/metrics_router.py`

```python
"""Metrics endpoints - feedback, usage, and quality aggregation."""
from __future__ import annotations

from collections import Counter, defaultdict
from datetime import datetime, timedelta
from typing import Any, Dict, List

from fastapi import APIRouter, HTTPException, Query

from backend.services.feedback_service import get_feedback_log
from backend.services.history_service import LOG_PATH

router = APIRouter(prefix="/metrics", tags=["Metrics"])


def _load_query_log() -> List[dict]:
    """Load query_log.json safely."""
    try:
        if not LOG_PATH.exists():
            return []
        import json
        with open(LOG_PATH, "r", encoding="utf-8") as f:
            data = json.load(f)
            return data if isinstance(data, list) else []
    except Exception:
        return []


@router.post("/feedback")
def submit_feedback(
    request_id: str = Query(...),
    feedback: str = Query(...),
    source: str = Query("chat"),
):
    """
    Submit user feedback (positive/negative) for a response.
    Query params: request_id, feedback, source (optional).
    """
    if feedback not in ("positive", "negative"):
        raise HTTPException(400, "feedback must be 'positive' or 'negative'")
    if source not in ("chat", "image"):
        source = "chat"
    from backend.services.feedback_service import record_feedback
    record_feedback(request_id, feedback, source)
    return {"status": "recorded", "request_id": request_id}


@router.get("/usage")
def get_usage_metrics(days: int = Query(7, ge=1, le=365)) -> Dict[str, Any]:
    """
    Usage metrics from query_log.
    Returns agent distribution, query types, and daily counts.
    """
    logs = _load_query_log()
    if not logs:
        return {
            "total_requests": 0, "by_agent": {}, "by_type": {},
            "by_day": {}, "days": days,
        }

    cutoff_date = (datetime.utcnow() - timedelta(days=days)).date()
    agent_counts: Counter = Counter()
    type_counts: Counter = Counter()
    day_counts: Dict[str, int] = defaultdict(int)
    total = 0

    for entry in logs:
        ts_str = entry.get("timestamp", "")
        try:
            ts = datetime.fromisoformat(ts_str.replace("Z", "+00:00"))
            ts_date = ts.date() if hasattr(ts, "date") else ts
            if ts_date < cutoff_date:
                continue
            day_key = ts.strftime("%Y-%m-%d") if hasattr(ts, "strftime") else str(ts)[:10]
        except Exception:
            continue

        agent = entry.get("agent", "unknown")
        agent_counts[agent] += 1

        qtype = entry.get("type", "text")
        type_counts[qtype] += 1

        day_counts[day_key] += 1
        total += 1

    return {
        "total_requests": total,
        "by_agent": dict(agent_counts),
        "by_type": dict(type_counts),
        "by_day": dict(sorted(day_counts.items())),
        "days": days,
    }


@router.get("/quality")
def get_quality_metrics(days: int = Query(30, ge=1, le=365)) -> Dict[str, Any]:
    """
    Quality metrics from feedback_log.
    Returns satisfaction rate and counts.
    """
    feedback = get_feedback_log()
    if not feedback:
        return {
            "total_responses": 0, "positive": 0, "negative": 0,
            "satisfaction_rate": None, "days": days,
        }

    cutoff_date = (datetime.utcnow() - timedelta(days=days)).date()
    positive = 0
    negative = 0

    for entry in feedback:
        ts_str = entry.get("timestamp", "")
        try:
            ts = datetime.fromisoformat(ts_str.replace("Z", "+00:00"))
            ts_date = ts.date() if hasattr(ts, "date") else ts
            if ts_date < cutoff_date:
                continue
        except Exception:
            continue

        fb = entry.get("feedback", "")
        if fb == "positive":
            positive += 1
        elif fb == "negative":
            negative += 1

    total = positive + negative
    rate = round(100 * positive / total, 1) if total > 0 else None

    return {
        "total_responses": total,
        "positive": positive,
        "negative": negative,
        "satisfaction_rate": rate,
        "days": days,
    }
```

**设计要点**：

1. 指标全部是**日志的二次聚合**，不额外埋点——埋点只发生一次（Agent 基类）；
2. `days` 参数带边界校验（`ge=1, le=365`）——防恶意大查询拖垮服务；
3. 满意度用 `None` 而非 `0` 表示"无数据"，前端可区分展示。

### 步骤 3：升级健康检查 `backend/routes/health_router.py`

将实训 02 的基础版升级为**依赖全景探测**：

```python
from fastapi import APIRouter
from datetime import datetime, timedelta
import time

from backend.core.config import settings, langsmith_enabled
from backend.core.llm_client import get_llm

router = APIRouter(prefix="/health", tags=["Health"])

START_TIME = time.time()

def _format_uptime(seconds: int) -> str:
    """Convert seconds to HH:MM:SS."""
    return str(timedelta(seconds=seconds))


@router.get("", include_in_schema=False)
@router.get("/")
async def health_check():

    uptime_sec = int(time.time() - START_TIME)

    try:
        get_llm()
        model_ok = bool(settings.TEXT_MODEL_NAME) and bool(settings.GROQ_API_KEY)
        groq_status = "configured" if model_ok else "not_configured"
    except Exception as e:
        groq_status = f"error: {str(e)}"

    langsmith_ok = langsmith_enabled()
    pinecone_ok = bool(settings.PINECONE_API_KEY) and bool(settings.PINECONE_INDEX_NAME)

    from backend.core.memory_manager import redis_available
    redis_ok = redis_available()

    return {
        "status": "OK",
        "service": "AgriGPT Backend",
        "timestamp_utc": str(datetime.utcnow().isoformat()),
        "uptime_seconds": uptime_sec,
        "uptime_hhmmss": _format_uptime(uptime_sec),
        "models": {
            "text_model": str(settings.TEXT_MODEL_NAME or ""),
            "vision_model": str(settings.VISION_MODEL_NAME or ""),
        },
        "dependencies": {
            "groq_api": groq_status,
            "langsmith": "enabled" if langsmith_ok else "disabled",
            "pinecone_rag": "configured" if pinecone_ok else "faiss (local)",
            "redis_memory": "connected" if redis_ok else "in-memory",
        },
        "notes": "Health OK",
    }
```

### 步骤 4：挂载并验证

`main.py`：

```python
from backend.routes.metrics_router import router as metrics_router
app.include_router(metrics_router)
```

验证序列：

```powershell
# 1. 先产生几条问答（任意 /ask/text）
# 2. 用量指标
curl.exe http://localhost:8000/metrics/usage?days=7
# 3. 对某个 request_id 点赞
curl.exe -X POST "http://localhost:8000/metrics/feedback?request_id=<刚才的id>&feedback=positive&source=chat"
# 4. 质量指标
curl.exe http://localhost:8000/metrics/quality
# 5. 全景健康
curl.exe http://localhost:8000/health
```

### 步骤 5（选做）：接入 LangSmith 全链路追踪

1. 注册 [smith.langchain.com](https://smith.langchain.com)，Settings → API Keys 创建 Key（`lsv2_pt_` 开头）；
2. `.env` 配置：

```dotenv
LANGCHAIN_TRACING_V2=true
LANGSMITH_API_KEY=lsv2_pt_xxx
LANGCHAIN_PROJECT=agrigpt
```

3. 重启后提问，即可在 LangSmith 面板看到：路由调用、每个 Agent 的 prompt/response、耗时、Token——**每一步都可回放**。

> 注意：LangSmith Key 与 Groq Key 无关，来自 smith.langchain.com。

---

## 三、验收标准

| # | 检查项 | 通过标准 |
|---|--------|----------|
| 1 | `/metrics/usage` | `by_agent` 能反映你实际测过的 Agent 分布 |
| 2 | 提交 feedback=maybe | 400 拒绝（枚举校验） |
| 3 | `/metrics/quality` | 满意度计算正确（手算核对） |
| 4 | `/health` | 四类依赖状态与实际配置一致（可故意停 Redis 验证） |
| 5 | `feedback_log.json` | 原子写入，无临时文件残留 |

---

## 四、常见问题排查

| 现象 | 原因与解决 |
|------|-----------|
| `by_agent` 全是 FormatterAgent | 正常——Formatter 也会落日志；如需只统计专家，聚合时过滤 `FormatterAgent` |
| 满意度一直是 None | 还没有任何反馈记录；或时间窗 `days` 小于数据时间 |
| LangSmith 无数据 | `LANGCHAIN_TRACING_V2` 必须为 `true` 且 Key 有效；查看启动日志 |

---

## 五、拓展任务（选做）

1. 把用量/质量指标做成简单的 HTML 报表页（后端返回数据 + 前端 recharts 画图）；
2. 给日志文件按天分片（`query_log_2025-08-28.json`），替代大小轮转；
3. 设计"负反馈闭环"：点踩的记录自动附带当时的 query，便于运营复盘（修改 `record_feedback` 签名与前端传参）。

> **下一节预告**：实训 10 开启前端线——Vite + React + Tailwind 工程初始化。
