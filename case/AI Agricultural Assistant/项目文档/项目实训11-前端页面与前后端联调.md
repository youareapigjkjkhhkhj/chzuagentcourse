# 项目实训11｜前端页面与前后端联调

> **训练目标**：实现聊天、图片诊断、历史三大页面；完成 Zustand 状态管理与 API 封装；全链路联调验收。
> **预计时长**：8 学时 ｜ **前置条件**：实训 10 完成
> **本次交付物**：`store/chatStore.ts`、`api/agriApi.ts`、三大页面与配套组件、天气头部组件

---

## 一、知识点

### 1. 状态管理边界

| 数据 | 放哪里 | 原因 |
|------|--------|------|
| 聊天消息、加载态 | Zustand（`chatStore`） | 跨页面共享（Chat 与 History 都要读） |
| 图片预览、拖拽状态 | 组件内 `useState` | 页面私有，出页面即销毁 |
| 服务端数据 | React Query / 直接 await | 请求生命周期短，不必进全局 |

### 2. 会话标识（与后端记忆呼应）

前端用 `localStorage` 保存 `session_id`（`crypto.randomUUID()` 生成），每次请求随 FormData 上传——这就是实训 08 后端记忆的"钥匙"。

### 3. 上传校验要"两端对齐"

后端限制（JPEG/PNG、8MB）前端要**先拦截一次**：避免无效大文件浪费上传带宽，报错也更友好。但后端校验绝不可省——前端校验可以被绕过。

---

## 二、操作步骤

### 步骤 1：实现聊天状态 `src/store/chatStore.ts`

```ts
import { create } from "zustand";

export interface ChatMessage {
  id: string;
  role: "user" | "assistant";
  content: string;
  timestamp: string;
  imageUrl?: string;
  agent?: string;
  requestId?: string;
}

interface ChatState {
  messages: ChatMessage[];
  isLoading: boolean;
  error: string | null;

  addMessage: (message: Omit<ChatMessage, "id" | "timestamp">) => void;
  setLoading: (loading: boolean) => void;
  setError: (error: string | null) => void;
  clearChat: () => void;
  addImageMessage: (file: File) => void;
}

export const useChatStore = create<ChatState>((set) => ({
  messages: [],
  isLoading: false,
  error: null,

  addMessage: (message) =>
    set((state) => {
      const limited = state.messages.slice(-200);
      return {
        messages: [
          ...limited,
          {
            ...message,
            id: crypto.randomUUID(),
            timestamp: new Date().toISOString(),
          },
        ],
      };
    }),

  setLoading: (loading) => set({ isLoading: loading }),
  setError: (error) => set({ error }),

  clearChat: () => {
    set((state) => {
      state.messages.forEach((msg) => {
        if (msg.imageUrl?.startsWith("blob:")) {
          URL.revokeObjectURL(msg.imageUrl); // 释放 blob，防止内存泄漏
        }
      });
      return { messages: [], error: null };
    });
  },

  addImageMessage: (file) =>
    set((state) => {
      const imageUrl = URL.createObjectURL(file);
      return {
        messages: [
          ...state.messages,
          {
            id: crypto.randomUUID(),
            role: "user",
            content: "",
            timestamp: new Date().toISOString(),
            imageUrl,
          },
        ],
      };
    }),
}));
```

**设计要点**：消息上限 200 条（`slice(-200)`）防止长对话内存膨胀；`requestId` 存入消息——反馈按钮要用。

### 步骤 2：实现 API 封装 `src/api/agriApi.ts`

```ts
import { apiClient } from "./client";

export interface ApiResponse {
  analysis: string;
  request_id?: string;
  status?: string;
  input?: any;
}

interface FormDataPayload {
  [key: string]: string | Blob | undefined;
}

// Session Management
const SESSION_KEY = 'agrigpt_session_id';

function getSessionId(): string {
  let sid = localStorage.getItem(SESSION_KEY);
  if (!sid) {
    sid = crypto.randomUUID
      ? crypto.randomUUID()
      : Date.now().toString(36) + Math.random().toString(36).substr(2);
    localStorage.setItem(SESSION_KEY, sid);
  }
  return sid;
}

function buildFormData(payload: FormDataPayload): FormData {
  const fd = new FormData();
  fd.append("session_id", getSessionId());

  Object.entries(payload).forEach(([key, value]) => {
    if (value !== undefined && value !== null) {
      fd.append(key, value);
    }
  });
  return fd;
}

async function postFormData(url: string, formData: FormData): Promise<ApiResponse> {
  const res = await apiClient.post<ApiResponse>(url, formData, {
    headers: { "Content-Type": "multipart/form-data" },
  });
  return res.data;
}

// Text-only request
export const askText = (query: string): Promise<ApiResponse> => {
  const payload = buildFormData({ query: query.trim() });
  return postFormData("/ask/text", payload);
};

// Image-only diagnosis
export const askImage = (image: File): Promise<ApiResponse> =>
  postFormData("/ask/image", buildFormData({ file: image }));

// Multimodal
export const askChat = (query: string, image?: File): Promise<ApiResponse> => {
  const payloadObj: FormDataPayload = { query: query.trim() };
  if (image) {
    payloadObj.file = image;
  }
  const payload = buildFormData(payloadObj);
  return postFormData("/ask/chat", payload);
};

// Feedback for quality metrics
export const submitFeedback = (
  requestId: string,
  feedback: "positive" | "negative",
  source: "chat" | "image" = "chat"
): Promise<{ status: string }> =>
  apiClient.post("/metrics/feedback", null, {
    params: { request_id: requestId, feedback, source },
  }).then((r) => r.data);
```

> 注：请求拦截器会在发送前把这里的 `Content-Type` 删掉（实训 10），由浏览器生成带 boundary 的正确头。

### 步骤 3：实现聊天页 `src/pages/Chat.tsx`

页面职责：渲染消息流、调用 `askChat`、错误提示、滚动到底部。核心发送逻辑：

```tsx
const handleSend = async (text: string, image?: File) => {
  try {
    setLoading(true);
    setError(null);

    if (image) addImageMessage(image);
    if (text.trim().length > 0) addMessage({ role: "user", content: text });

    const response = await askChat(text, image);

    addMessage({
      role: "assistant",
      content: response?.analysis ?? "",
      agent: "AgriGPT",
      requestId: response?.request_id,
    });

    toast.success("Response received");
  } catch (error: any) {
    const errorMessage = error?.message ?? "Failed to get response";
    setError(errorMessage);
    toast.error(errorMessage);
  } finally {
    setLoading(false);
  }
};
```

页面结构（完整实现参考源码）：

```
<div h-计算高度 flex-col>
  ├─ 消息滚动区（空态显示 EmptyState 建议问题，否则渲染 ChatBubble 列表）
  │    └─ isLoading 时显示 Loader "AgriGPT is thinking..."
  └─ ChatInput（文本输入 + 图片选择 + 发送 + 清空）
```

配套组件职责（对照源码 `src/components/` 实现）：

| 组件 | 要点 |
|------|------|
| `ChatBubble` | 区分用户/助手气泡；助手内容用 `react-markdown + remark-gfm` 渲染；带 `requestId` 时显示 👍/👎 调 `submitFeedback` |
| `ChatInput` | 受控输入；`<input type="file" accept="image/jpeg,image/png">`；Enter 发送、Shift+Enter 换行 |
| `EmptyState` | 展示 3~4 个建议问题卡片，点击直接发送 |
| `Loader` | 三点动画 + 文案 |

> 验收重点：助手回答包含 `###` 标题与加粗（实训 05 Formatter 的产出），Markdown 必须正确渲染。

### 步骤 4：实现图片诊断页 `src/pages/ImageDiagnosis.tsx`

状态机：**未选图 → 已选图（预览）→ 诊断中 → 出结果（可反馈）**。

关键逻辑（前后端校验对齐）：

```tsx
const processFile = (file: File) => {
  const allowed = ["image/jpeg", "image/png"];
  if (!allowed.includes(file.type)) {
    toast.error("Please select a JPG or PNG image (max 8MB)");
    return;
  }
  if (file.size > 8 * 1024 * 1024) {
    toast.error("Image size must be less than 8MB");
    return;
  }
  setSelectedImage(file);
  if (previewUrl) URL.revokeObjectURL(previewUrl);
  setPreviewUrl(URL.createObjectURL(file));
  setResult(null);
};
```

其余要点（完整实现参考源码）：

1. **拖拽上传**：`onDrop`/`onDragOver`/`onDragLeave` + `isDragOver` 高亮；
2. **诊断**：`askImage(selectedImage)` → Markdown 渲染结果；
3. **反馈闭环**：结果下方 👍/👎 → `submitFeedback(lastRequestId, value, "image")`，提交后禁用；
4. **资源清理**：切换/卸载组件时 `URL.revokeObjectURL` 释放预览。

### 步骤 5：实现历史页 `src/pages/History.tsx`

读取同一个 `chatStore`，只展示用户消息卡片并关联其后最近的助手回复：

```tsx
const userMessages = messages.filter((m) => m.role === "user");

const getMessageType = (msg) => {
  const hasText = msg.content && msg.content.trim() !== "";
  const hasImage = !!msg.imageUrl;
  if (hasText && hasImage) return { type: "Multimodal", ... };
  if (hasImage) return { type: "Image", ... };
  return { type: "Text", ... };
};

const findAssistantReply = (userMsg) =>
  messages.find(
    (m) => m.role === "assistant" &&
      new Date(m.timestamp).getTime() > new Date(userMsg.timestamp).getTime()
  );
```

完整卡片布局（类型徽标、时间戳、回答截断预览、图片缩略图）参考源码。

### 步骤 6：天气头部组件 `src/components/WeatherHeader.tsx`

实现思路：

```tsx
useEffect(() => {
  if (!navigator.geolocation) return;
  navigator.geolocation.getCurrentPosition(async (pos) => {
    try {
      const res = await apiClient.get("/weather/current", {
        params: { lat: pos.coords.latitude, lon: pos.coords.longitude },
      });
      setWeather(res.data);
    } catch { /* 静默降级，不影响主功能 */ }
  });
}, []);
```

展示：地名 + 温度 + 湿度 + 风速 + 按 `condition`（sunny/cloudy/rainy）切换图标。后端未配置天气 Key 时隐藏组件。

### 步骤 7：全链路联调

双端同时启动（两个终端）：

```powershell
# 终端1：后端（站在项目根）
uvicorn backend.main:app --reload

# 终端2：前端
cd frontend; npm run dev
```

按《联调验收清单》（见下）逐项执行，遇到 400/413/415/500 时先看后端控制台与浏览器 Network 面板定位归属端。

---

## 三、联调验收清单

| # | 场景 | 预期 |
|---|------|------|
| 1 | 首页发文本问题 | 气泡流出现问答，Markdown 渲染正常 |
| 2 | 连续追问（代词） | 第二轮回答延续上下文（后端记忆生效） |
| 3 | 图文混合发送 | 用户气泡含图片缩略，回答含诊断内容 |
| 4 | 图片诊断页拖拽上传 | 预览出现；点诊断出结果；👍 后按钮禁用 |
| 5 | 上传 .gif / 超大图 | 前端 toast 拦截，未发请求 |
| 6 | 历史页 | 展示所有用户提问及类型徽标 |
| 7 | 清空对话 | 消息清空、blob 释放 |
| 8 | 后端停掉再发送 | 前端 toast 显示归一化错误文案，不白屏 |
| 9 | 后端 `/metrics/quality` | 页面点赞后 positive 计数 +1 |
| 10 | 刷新页面 | 前端消息清空（设计如此），但同 `session_id` 后端记忆仍在，追问仍连贯 |

---

## 四、常见问题排查

| 现象 | 原因与解决 |
|------|-----------|
| 回答不渲染标题/加粗 | 未接 `react-markdown`；或后端返回被 HTML 转义 |
| 点赞 400 | `request_id` 为空——确认消息保存了后端返回的 `request_id` |
| 刷新后追问"失忆" | `localStorage` 的 `session_id` 被清了，或后端没用 Redis |
| 图片预览内存涨 | 检查 `revokeObjectURL` 是否在切换图片与卸载时都执行 |
| 天气组件一直转圈 | 浏览器拒绝定位权限——属预期，做好静默降级 |

---

## 五、拓展任务（选做）

1. 用 `localStorage` 持久化前端消息，刷新不丢（注意与后端记忆的一致性）；
2. 给助手气泡加"复制回答"按钮；
3. 把 `/metrics/usage` 做成一个隐藏管理页（图表用 `recharts`），展示各 Agent 用量饼图。

> **下一节预告**：实训 12 收官——自动化测试全绿 + Docker 一键部署 + 云上线。
