# P6 · 安全与用量统计 ✅ 已完成（2026-08-29）

> 周期参考：约 1 周 ｜ 前置：P1（建议放在 P3–P5 之后收尾）｜ 产出：密钥零接触 + 成本可视
>
> **🎉 本期达成后，《设计方案》§14 七条成功标准全部完成，产品规划收官。**

## 落地摘要

| # | 任务 | 落地位置 | 说明 |
| --- | --- | --- | --- |
| 1 | 密钥迁移 | `agent-core/store/keyStore.ts` + `main/keyVault.ts` | `~/.AgentBuddy/keys.json` 仅存密文（Electron `safeStorage`，`ss1:` 前缀；不可用时回退 `obf1:` base64 混淆）；`migrate()` 启动时把 config.json 明文一次性迁出（`apiKey=''、encrypted=true`）；Renderer 仅见 `apiKeyMask`，编辑留空 = 保留原 Key，输入新值 = 重新设置 |
| 2 | 日志脱敏 | `agent-core/redact.ts` | `registerSecret`（<6 字符忽略）/ `redact`（`***` 全量替换）/ `logger`（info/warn/error 字符串先脱敏）；KeyStore 解密、chatService getKey、MCP `${VAR}` env 解析值均自动登记 |
| 3 | 用量采集 | `agent-core/llm/usage.ts`（P0 已有）+ `usage/pricing.ts` + `usage/store.ts` | 三厂商缓存字段归一（OpenAI/DeepSeek/Anthropic）；`MODEL_PRICING` 六模型 $/1M 计价（精确→子串→未知 0 不报错）；每次响应追加 `usage.jsonl` |
| 4 | 执行记录 | `orchestrator.ts` onRecord 钩子 + `chatService` 落盘 | 五终态（未知工具 / 入参非法 / 权限拒绝 / 成功 / 失败）各落一条 `{ ts, sessionId, tool, target, risk, ok, ms }` → `records.jsonl` |
| 5 | 聚合接口 | `usage/store.ts` + IPC `usage:stats` / `usage:records` | `stats(days?)` 总量/近7日/按模型/按工具/会话数/缓存命中率；`records({sessionId?,tool?})` 组合筛选 + 倒序 + 上限 2000；畸形行跳过；空数据零值视图 |
| 6 | 用量统计页 | `renderer/views/UsageView.vue` | 三 Tab：总览五卡 + 7 日柱状图 + 工具分布横条；模型消耗卡（请求/费用/输入/输出/缓存/命中率 + 占比条）；执行记录表（会话 × 工具双下拉筛选 + 风险徽标 + 空占位） |
| 7 | 侧栏入口 | `App.vue` | 导航与底部 chart 按钮指向用量统计；PlaceholderView 已删除 |

## 验收条件

- [x] 《设计方案》成功标准 4：刷新应用后会话与配置仍在；API Key 只以掩码出现，磁盘与日志中无明文（`keyStore.test` 断言 keys.json / config.json 均不含明文；`redact.test` 断言脱敏）
- [x] 《设计方案》成功标准 6：用量统计可按模型查看 Token 消耗、按会话 / 工具筛选回放执行记录
- [x] 缓存命中率正确：支持缓存的模型显示命中占比，不支持的显示 0% 而非报错（`usageStore.test` 空目录/单模型 0 输入用例）
- [x] 估算费用 = 各模型单价 × 用量累加，与手工核算一致（`pricing.test`：(800×0.25 + 200×0.025 + 500×2.5)/1e6 = 0.001455 等）
- [x] 执行记录筛选组合（某会话 × 某工具）结果正确；空结果有占位提示（`usageStore.test` + UI「无匹配的执行记录」）
- [x] 大量记录（≥1 万行）下聚合接口响应 <200ms（`usageStore.test` 性能用例：1 万 usage + 1 万 records 实测通过）
- [x] 单测：缓存字段归一（三家厂商各一例）、掩码生成、脱敏断言（`usage.test` / `redact.test` / `pricing.test` / `keyStore.test`）

## 交付检查点

- [x] 真模型冒烟：`smoke.real.test.ts` 3 用例全过（修 bug 全流程 / /review 技能 / MCP 工具 NETWORK 放行），usage 归一与落盘链路同构于 chatService。

## 测试结果

- typecheck：agent-core + desktop 双包零错误
- 单测：248 passed | 3 skipped（smoke 无凭据时跳过；带凭据复跑 3/3 过）
- build：renderer 472KB / main 778KB，vite + esbuild 通过
