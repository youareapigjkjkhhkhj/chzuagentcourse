"""课程的读写（P1 §3 / P1-C1）。

`generation/` 负责**产出**内容，`courses/` 负责**存放**内容。分开的理由是：
生成管线只在「用户点了生成」时跑一次，而课程的读写被工作台、编辑区、导出、
课堂运行时反复使用（P1-P5）。把「怎么存」摊在管线里，后面每个消费方都得
自己再拼一遍同一套关系。
"""

from app.services.courses import store

__all__ = ["store"]
