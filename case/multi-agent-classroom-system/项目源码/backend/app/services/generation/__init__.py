"""课程生成内核（P1）。

分三层，各自只做一件事：

    schema.py  页面 DSL 的校验与规范化 —— 「什么样的一页算合格」
    llm.py     带校验与记账的模型调用 —— 「怎么把不合格的救回来、怎么记账」
    pipeline.py 三段式生成与状态机（P1-3）

放在一个包里而不是摊平在 services/ 下，是因为它们的消费者只有彼此：
生成管线的每一步都先过 schema、再走 llm，外面没有第二个调用方。
"""
