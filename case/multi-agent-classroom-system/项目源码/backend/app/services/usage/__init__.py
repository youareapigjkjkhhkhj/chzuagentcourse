"""用量账本、聚合与预算（P5 §4.2 / F5-7、F5-8）。

四件事分在四个模块里，各管一段：

    pricing.py    单价 → 金额（估算）
    ledger.py     写：model_calls 的唯一落笔处（LLM 与三条语音链路都走它）
    aggregate.py  读：看板与单课明细（含 daily_usage 日缓存）
    budget.py     拦：超限拒绝新任务（F5-8）

`days.py` 是它们共用的日界（本地日 ⇄ UTC 窗口），不是第五件事。

### 两个账本

| 表 | 记的是 | 谁写 |
|----|--------|------|
| `model_calls` | **调用发生了一次** —— 耗时、成败、错误码、token | `ledger.record()` |
| `usage_records` | **按量计费的东西用了多少** —— 字符 / 秒 + 金额 | `voice/usage.record()`（P2 就在写） |

一次 TTS 两张表都写一行，这不是重复记账；**金额与用量逐链路各认一个来源**
（LLM 取前者、语音取后者），跨表相加不会把语音算两遍。
这条口径是 `aggregate` 与 `budget` 算数的地基，写在 P5 文档 §10.2。

### 金额永远是估算

价目表是本地的（配置 + 设置页），不是账单。所有对外字段都叫 `estCost`、
文案都写「估算费用」—— 别让任何人在对账时才发现这件事。
"""
