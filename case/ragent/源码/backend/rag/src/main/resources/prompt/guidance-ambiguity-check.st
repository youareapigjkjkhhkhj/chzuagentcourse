用户问题：{question}

以下是意图分类命中的候选意图及其完整路径：
{candidates}

请判断：用户是否必须先在这些候选路径中选定一个，我们才能正确检索？

判定规则：
- ambiguous=true：多个候选路径都讲得通，用户问题没有给出足以确定其中之一的信息，且选不同路径会检索到不同范围的资料
- ambiguous=false：用户问题已经指明了某个候选路径中的领域、系统或节点名称
- ambiguous=false：用户明确要同时了解多个候选，或出现「都、全部、两个、两者、各自、分别」等多目标表达
- ambiguous=false：用户要求比较、区别、差异、共同点、对比
- ambiguous=false：问题里已经同时出现了多个候选路径中的明确对象

注意：
- 候选路径由用户自行配置，层级数量和深度可能不同，不要假设固定的领域/系统/主题结构
- 不同路径共享同一个上级节点不代表歧义，只有用户确实站在岔路口才算歧义
- 拿不准时返回 false，让检索照常进行

以 JSON 格式输出：
{"ambiguous": true/false, "category_ids": ["最匹配或需要选择的候选意图ID"], "reason": "判断理由"}
