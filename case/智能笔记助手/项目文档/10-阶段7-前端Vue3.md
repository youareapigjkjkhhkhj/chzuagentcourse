# 阶段 7 · 前端 Vue3（笔记管理 + 流式问答界面）

> 本阶段目标：用 Vue 3 + Vite + axios 实现 Web 界面——左侧管理笔记/构建知识库，右侧提问并流式展示回答与来源。**本阶段结束，`npm run build` 产出 `dist/`，由阶段 6 的 Flask 同源托管，访问 `http://127.0.0.1:5000` 即为完整应用。**

---

## 7.1 目标

- 前端工程：`package.json`（vue3.4 / vite5 / axios1.7）、`vite.config.js`（开发态 `/api` 代理到 5000）、`index.html`、`main.js`、`style.css`。
- 接口封装 `api.js`：笔记增删查、建库、状态、非流式 `askQA`、流式 `streamQA`（原生 `fetch` 解析 SSE）。
- 两个组件：`NotePanel.vue`（上传/列表/构建）、`ChatPanel.vue`（流式问答 + 来源 chips）。
- **可运行验证**：`npm install`（npmmirror）+ `npm run build` 生成 `dist/`；Flask `/` 返回前端 HTML（200）。

---

## 7.2 前置

- 阶段 6：后端 `app.py` 已能同源托管 `frontend/dist`。
- 已安装 Node ≥ 18 / npm ≥ 9。

---

## 7.3 核心代码

### 7.3.1 工程配置

```json
// 源码/frontend/package.json:1-19（节选）
{
  "name": "notes-assistant-frontend",
  "type": "module",
  "scripts": { "dev": "vite", "build": "vite build", "preview": "vite preview --port 4173" },
  "dependencies": { "axios": "^1.7.7", "vue": "^3.4.38" },
  "devDependencies": { "@vitejs/plugin-vue": "^5.1.3", "vite": "^5.4.6" }
}
```

```js
// 源码/frontend/vite.config.js:1-21（全）
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
export default defineConfig({
  plugins: [vue()],
  server: {
    port: 5173,
    proxy: { '/api': { target: 'http://localhost:5000', changeOrigin: true } }  // 开发态代理
  },
  build: { outDir: 'dist', chunkSizeWarningLimit: 1500 }
})
```

### 7.3.2 接口封装 `src/api.js`（含 SSE 流式解析）

```js
// 源码/frontend/src/api.js:1-68（节选）
import axios from 'axios'
const http = axios.create({ baseURL: '/api' })        // 行 3

export async function listNotes() { const { data } = await http.get('/notes'); return data.notes || [] }
export async function uploadNote(file) { /* FormData -> /notes/upload */ }
export async function deleteNote(id) { await http.delete('/notes/' + encodeURIComponent(id)) }
export async function buildIndex() { const { data } = await http.post('/index/rebuild'); return data }
export async function getIndexStatus() { const { data } = await http.get('/index/status'); return data }

export async function askQA(question, topK) {        // 行 31 非流式
  const { data } = await http.post('/qa', { question, top_k: topK })
  if (data.error) throw new Error(data.error)
  return data
}

export async function streamQA(question, topK, onToken, onDone, onError) {  // 行 38 流式
  const resp = await fetch('/api/qa/stream', {
    method: 'POST', headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ question, top_k: topK })
  })
  const reader = resp.body.getReader()
  const decoder = new TextDecoder()
  let buf = ''
  while (true) {
    const { done, value } = await reader.read()
    if (done) break
    buf += decoder.decode(value, { stream: true })
    const parts = buf.split('\n\n'); buf = parts.pop()
    for (const p of parts) {
      if (!p.startsWith('data: ')) continue
      const obj = JSON.parse(p.slice(6))               // 解析 SSE 帧
      if (obj.type === 'token') onToken(obj.content)
      else if (obj.type === 'done') onDone(obj.sources || [])
      else if (obj.type === 'error') onError(obj.message)
    }
  }
}
```

### 7.3.3 笔记面板 `NotePanel.vue`（上传 + 构建）

```vue
<!-- 源码/frontend/src/components/NotePanel.vue:1-86（节选） -->
<input type="file" accept=".md,.txt,.pdf,.markdown" @change="onFile" ref="fileInput" />
<button :disabled="!file || uploading" @click="upload">{{ uploading ? '上传中…' : '上传笔记' }}</button>
<button class="primary" :disabled="building" @click="build">{{ building ? '构建中…' : '构建知识库' }}</button>
<!-- 列表 + 删除 -->
<script setup>
import { ref, onMounted } from 'vue'
import { listNotes, uploadNote, deleteNote, buildIndex } from '../api'
async function build() {
  const r = await buildIndex()
  buildMsg.value = `已构建：${r.note_count} 篇笔记 / ${r.chunk_count} 切片 / ${r.vector_count} 向量`
  emit('changed')
}
</script>
```

### 7.3.4 问答面板 `ChatPanel.vue`（流式展示 + 来源）

```vue
<!-- 源码/frontend/src/components/ChatPanel.vue:1-81（节选） -->
<label class="toggle"><input type="checkbox" v-model="useStream" /> 流式输出</label>
<div class="messages"> ... 来源 chips ... </div>
<script setup>
import { ref } from 'vue'
import { askQA, streamQA } from '../api'
async function send() {
  // 推入 user/assistant 消息
  if (useStream.value) {
    await streamQA(q, null,
      (tok) => { assistant.content += tok },                 // 逐字追加
      (sources) => { assistant.sources = sources },          // 结束补来源
      (msg) => { assistant.content = '⚠️ ' + msg })
  } else {
    const data = await askQA(q, null)
    assistant.content = data.answer; assistant.sources = data.sources || []
  }
}
</script>
```

> `App.vue` 负责顶部品牌栏 + 知识库状态指示灯，并组合 `NotePanel` 与 `ChatPanel`（`源码/frontend/src/App.vue:1-37`）。

---

## 7.4 实操步骤（安装 + 构建）

```bash
cd 智能笔记助手/源码/frontend
npm install --registry=https://registry.npmmirror.com
npm run build          # 产出 dist/
```

启动后端（阶段 6 已成型）：

```bash
cd 智能笔记助手/源码/backend
python app.py
# 浏览器打开 http://127.0.0.1:5000
```

---

## 7.5 可运行验证（本阶段 checkpoint）

```bash
ls 源码/frontend/dist/      # 应有 index.html / assets/...
```

后端 `/` 返回 200 且为前端 HTML：

```bash
curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:5000/
# 200
curl -s http://127.0.0.1:5000/ | head -c 60
# <!DOCTYPE html>...<title>智能笔记助手 · RAG</title>
```

**✅ 阶段 7 通过标准**：`npm run build` 成功产出 `dist/`；Flask `/` 返回 200 前端页；页面可上传笔记、构建知识库、流式提问并展示来源。

---

## 7.6 本阶段产物

```
源码/frontend/
├── package.json / vite.config.js / index.html
└── src/
    ├── main.js / App.vue / style.css / api.js
    └── components/NotePanel.vue / ChatPanel.vue
源码/frontend/dist/        # ★ 构建产物（Flask 同源托管）
```

> 阶段 8 做端到端/接口测试、常见问题与部署建议，收尾整个项目。

---

## 7.7 下一步

→ [阶段 8 · 集成测试与排错 →](./11-阶段8-集成测试与排错.md)
