# 实验9 医疗大模型知识蒸馏实践

> **类型**：⚠️拓展实验，需补充代码 | **难度**：★★★★★ | **时长**：3小时

## 实验目的
掌握大模型知识蒸馏，使用大参数量医疗模型作为教师，Qwen2‑4B作为学生做蒸馏。

## 实验原理
知识蒸馏（Knowledge Distillation）通过大模型（教师）指导小模型（学生）学习，将教师模型的知识迁移到学生模型。MedicalGPT无蒸馏脚本，需要自行实现。

> **说明**：MedicalGPT无蒸馏脚本，需要自行实现，拓展实验。

## 实验内容

### 步骤1：构建蒸馏数据集
```python
# create_distill_dataset.py - 构建蒸馏数据集
import json
from transformers import AutoModelForCausalLM, AutoTokenizer
import torch

def generate_teacher_responses(teacher_model_path, input_data_path, output_path):
    """使用教师模型生成回复，构建蒸馏数据集"""
    tokenizer = AutoTokenizer.from_pretrained(teacher_model_path, trust_remote_code=True)
    model = AutoModelForCausalLM.from_pretrained(
        teacher_model_path, torch_dtype=torch.bfloat16, device_map="auto", trust_remote_code=True
    )
    
    distill_data = []
    with open(input_data_path, 'r', encoding='utf-8') as f:
        for line in f:
            data = json.loads(line.strip())
            query = data['conversations'][0]['value']
            
            messages = [
                {"role": "system", "content": "你是一个专业的医疗科普助手。"},
                {"role": "user", "content": query}
            ]
            text = tokenizer.apply_chat_template(messages, tokenize=False, add_generation_prompt=True)
            inputs = tokenizer(text, return_tensors="pt").to(model.device)
            
            with torch.no_grad():
                outputs = model.generate(**inputs, max_new_tokens=512, temperature=0.7)
            
            response = tokenizer.decode(outputs[0][inputs.input_ids.shape[1]:], skip_special_tokens=True)
            
            distill_data.append({
                "conversations": [
                    {"from": "human", "value": query},
                    {"from": "gpt", "value": response}
                ]
            })
    
    with open(output_path, 'w', encoding='utf-8') as f:
        for item in distill_data:
            f.write(json.dumps(item, ensure_ascii=False) + '\n')
    
    print(f"蒸馏数据集已生成: {output_path}")
    print(f"样本数量: {len(distill_data)}")

if __name__ == "__main__":
    generate_teacher_responses(
        teacher_model_path="Qwen/Qwen2.5-7B-Instruct",
        input_data_path="data/sft/medical_sft_1K_format.jsonl",
        output_path="data/sft/medical_distill_7b.jsonl"
    )
```

### 步骤2：实现知识蒸馏训练
```python
# distillation_training.py - 知识蒸馏训练脚本
import torch
import torch.nn as nn
import torch.nn.functional as F
from transformers import AutoModelForCausalLM, AutoTokenizer, TrainingArguments, Trainer
from datasets import load_dataset

class DistillationLoss(nn.Module):
    """蒸馏损失函数"""
    def __init__(self, temperature=2.0, alpha=0.5):
        super().__init__()
        self.temperature = temperature
        self.alpha = alpha
    
    def forward(self, student_logits, teacher_logits, labels):
        distillation_loss = F.kl_div(
            F.log_softmax(student_logits / self.temperature, dim=-1),
            F.softmax(teacher_logits / self.temperature, dim=-1),
            reduction='batchmean'
        ) * (self.temperature ** 2)
        
        ce_loss = F.cross_entropy(
            student_logits.view(-1, student_logits.size(-1)),
            labels.view(-1),
            ignore_index=-100
        )
        
        return self.alpha * distillation_loss + (1 - self.alpha) * ce_loss

def distill(teacher_model_path, student_model_path, train_data_path, output_path):
    """执行知识蒸馏训练"""
    teacher_tokenizer = AutoTokenizer.from_pretrained(teacher_model_path, trust_remote_code=True)
    teacher_model = AutoModelForCausalLM.from_pretrained(
        teacher_model_path, torch_dtype=torch.bfloat16, device_map="auto", trust_remote_code=True
    )
    teacher_model.eval()
    
    student_tokenizer = AutoTokenizer.from_pretrained(student_model_path, trust_remote_code=True)
    student_model = AutoModelForCausalLM.from_pretrained(
        student_model_path, torch_dtype=torch.bfloat16, device_map="auto", trust_remote_code=True
    )
    
    dataset = load_dataset('json', data_files=train_data_path)
    
    training_args = TrainingArguments(
        output_dir=output_path,
        num_train_epochs=3,
        per_device_train_batch_size=2,
        gradient_accumulation_steps=8,
        learning_rate=5e-5,
        logging_steps=10,
        save_steps=500,
        bf16=True,
    )
    
    class DistillationTrainer(Trainer):
        def __init__(self, teacher_model=None, **kwargs):
            super().__init__(**kwargs)
            self.teacher_model = teacher_model
            self.distill_loss = DistillationLoss()
        
        def compute_loss(self, model, inputs, return_outputs=False, **kwargs):
            outputs = model(**inputs)
            student_logits = outputs.logits
            
            with torch.no_grad():
                teacher_outputs = self.teacher_model(**inputs)
                teacher_logits = teacher_outputs.logits
            
            loss = self.distill_loss(student_logits, teacher_logits, inputs['labels'])
            return (loss, outputs) if return_outputs else loss
    
    trainer = DistillationTrainer(
        teacher_model=teacher_model,
        model=student_model,
        args=training_args,
        train_dataset=dataset["train"],
        tokenizer=student_tokenizer,
    )
    
    trainer.train()
    student_model.save_pretrained(output_path)
    student_tokenizer.save_pretrained(output_path)
    print(f"蒸馏训练完成，保存到: {output_path}")

if __name__ == "__main__":
    distill(
        teacher_model_path="Qwen/Qwen2.5-7B-Instruct",
        student_model_path="Qwen/Qwen2.5-4B-Instruct",
        train_data_path="data/sft/medical_distill_7b.jsonl",
        output_path="outputs-distill-medical-qwen"
    )
```

### 步骤3：对比蒸馏前后学生模型的医疗问答能力
```python
# compare_distill.py - 对比蒸馏前后效果
def compare_distillation():
    print("知识蒸馏效果对比:")
    print("-" * 60)
    print("蒸馏前: 学生模型直接使用基座权重")
    print("蒸馏后: 学生模型吸收了教师模型的知识")
    print("\n预期改善:")
    print("  - 医疗问答准确度提升")
    print("  - 回答专业性增强")
    print("  - 幻觉减少")

if __name__ == "__main__":
    compare_distillation()
```

## 实验产出
- 蒸馏数据集：`data/sft/medical_distill_7b.jsonl`
- 蒸馏后的学生模型：`outputs-distill-medical-qwen/`
- 蒸馏前后效果对比报告

## 思考题
1. 知识蒸馏中温度参数T的作用是什么？
2. 在医疗场景下，蒸馏可能导致哪些知识损失？
